import React from 'react';
import SightForm from './Form';
import ContainerLayout from 'Components/UI/ContainerLayout';

const CreateSight = () => (
  <ContainerLayout header="Добавяне на обект">
    <SightForm />
  </ContainerLayout>

);

export default CreateSight;
